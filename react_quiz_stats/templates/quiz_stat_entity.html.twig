{#
/**
 * @file quiz_stat_entity.html.twig
 * Default theme implementation to present Quiz stat entity data.
 *
 * This template is used when viewing Quiz stat entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_quiz_stat_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('quiz_stat_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
